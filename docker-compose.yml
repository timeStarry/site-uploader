services:
  site-uploader:
    build: .
    restart: unless-stopped
    user: "${APP_UID:-1000}:${APP_GID:-1000}"
    env_file: .env
    ports:
      - "${BIND_IP:-127.0.0.1}:${HOST_PORT:-8080}:${PORT:-8080}"
    volumes:
      - ./data:/data
